"""Context-packet builder — stub for Day 1-2.

Real implementation arrives on Day 4.
"""
from __future__ import annotations

from typing import Any


async def build_context_packet(
    client_id: str,
    month: str,
) -> dict[str, Any]:
    """TODO(Day 4): assemble client + portfolio + market + news context."""
    raise NotImplementedError("context_builder.build_context_packet — Day 4")
